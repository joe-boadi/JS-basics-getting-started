// Strings
var brand = "Joecode"
console.log(typeof brand)
console.log(brand.length)
console.log(brand.toUpperCase())
console.log(brand.substring(0, 6))

/*
    Working with strings allows us to apply a lot of methods to manipulate the data
*/

var a = "Joe"
var b = "code"
console.log(a + " " + b) // concantenating strings.
console.log(`${a} ${b}`) // usong back ticks.
console.log(`${a} ${b.toUpperCase()}`)