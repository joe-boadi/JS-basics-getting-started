//Arrays
someNames = [
    'arthur', 'bernard', 
    'colins', 'derrick', 
    'evans'
]

// looping through arrays.
 for(let i = 0; i < someNames.length; i++){
    console.log(`index ${[i]} ${someNames[i]}`);
 };
